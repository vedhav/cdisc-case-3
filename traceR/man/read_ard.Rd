% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_ard.R
\name{read_ard}
\alias{read_ard}
\title{Read a long-skinny Analysis Results Dataset (ARD) into a trace fragment}
\usage{
read_ard(path)
}
\arguments{
\item{path}{Path to the ARD CSV (or a data frame already read in).}
}
\value{
A \code{\link[=trace_graph]{trace_graph()}} fragment with \code{result} nodes.
}
\description{
Reads the reusable results-by-row ARD (the contract emitted by the
cdisc-case-3 executor and by \code{\link[=execute_analysis]{execute_analysis()}}): one row per statistic with
columns \verb{output_id, analysis_id, operation_id, group_var, group_level, variable, variable_level, stat_name, stat_label, stat_raw, stat_fmt}.
}
\details{
One \code{result} node is created per \code{analysis_id} (carrying every stat row in
its \code{attrs$stats} for querying), with a \code{produces} edge from the analysis
and \code{yields} edges from each contributing operation. This closes the
objective to result loop in the graph.
}
