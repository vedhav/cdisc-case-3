% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_ars.R
\name{read_ars}
\alias{read_ars}
\title{Read a CDISC ARS Reporting Event into a trace graph fragment}
\usage{
read_ars(path)
}
\arguments{
\item{path}{Path to the ARS Reporting Event JSON.}
}
\value{
A \code{\link[=trace_graph]{trace_graph()}} fragment.
}
\description{
Ingests an Analysis Results Standard (ARS) v1.0 \code{ReportingEvent} JSON:
its analyses, outputs, methods and operations, analysis sets, groupings and
data subsets. Edges capture the analysis wiring — which method, population,
groupings and subset each analysis uses, which ADaM \code{dataset.variable} it
operates on, and (by walking \code{mainListOfContents}) which output displays it.
}
\details{
ADaM variable nodes are created with the id scheme \verb{adam_var:<DATASET>.<VAR>}
so they merge with the nodes produced by \code{\link[=read_define]{read_define()}}.
}
