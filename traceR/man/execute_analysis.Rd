% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/execute.R
\name{execute_analysis}
\alias{execute_analysis}
\title{Execute one ARS analysis into a long-skinny ARD}
\usage{
execute_analysis(graph, analysis_id, adam_dir, output_id = NULL)
}
\arguments{
\item{graph}{A \code{\link[=trace_graph]{trace_graph()}} built from USDM+ARS(+define).}

\item{analysis_id}{An ARS analysis reference.}

\item{adam_dir}{Directory of ADaM CSVs (lowercase \verb{<dataset>.csv}).}

\item{output_id}{Optional output id to stamp on the ARD (else resolved from
the graph's \code{displayed_in} edge).}
}
\value{
A long-skinny ARD tibble, or \code{NULL} if unsupported.
}
\description{
Binds the analysis's dataset, variable, treatment grouping and population
straight from the trace graph, loads the matching ADaM CSV from \code{adam_dir},
and computes the statistics with the \code{cards} engine. Supported shapes:
subject counts by group (variable \code{USUBJID}), a continuous summary by group,
and a categorical summary by group. Model-based / hierarchical outputs
(ANCOVA, MMRM, Kaplan-Meier, AE-by-SOC/PT) are out of scope for the built-in
executor and return \code{NULL} with a reason attribute — they are the
AI-drafted, human-reviewed path handled by the sibling ars-to-tfl workflow.
}
