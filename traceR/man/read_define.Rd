% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_define.R
\name{read_define}
\alias{read_define}
\title{Read a define.xml into a trace graph fragment}
\usage{
read_define(path, standard = c("adam", "sdtm"))
}
\arguments{
\item{path}{Path to the define.xml file.}

\item{standard}{Which standard this define describes: \code{"sdtm"} or \code{"adam"}.
Controls node types (\code{sdtm_dataset}/\code{sdtm_var} vs \code{adam_dataset}/
\code{adam_var}).}
}
\value{
A \code{\link[=trace_graph]{trace_graph()}} fragment.
}
\description{
Parses a CDISC define.xml (Case Report Tabulation Data Definition) into
dataset, variable and codelist nodes plus their lineage edges. Handles both
define.xml 2.0 (where origin/derivation live in \code{def:Origin} and \code{MethodDef}
elements) and the older define 1.0 style used in the CDISC pilot ADaM
package (where \code{Origin} and the derivation source are \emph{attributes} on
\code{ItemDef}). XPath is written with \code{local-name()} so it is agnostic to the
document's namespace prefixes.
}
\details{
Variable nodes use the id scheme \verb{<vartype>:<DATASET>.<VAR>} so ADaM
variables merge with the \code{operates_on} targets produced by \code{\link[=read_ars]{read_ars()}}, and
\code{derived_from} edges (ADaM variable to its predecessor SDTM/ADaM variable)
are inferred from \code{Origin = "Predecessor"} and from \code{DATASET.VAR} tokens in
the derivation comment.
}
