% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_trace.R
\name{build_trace}
\alias{build_trace}
\title{Assemble a full traceability graph from CDISC source metadata}
\usage{
build_trace(
  usdm = NULL,
  ars = NULL,
  define = NULL,
  ard = NULL,
  endpoint_output_map = NULL
)
}
\arguments{
\item{usdm}{A path to a USDM JSON, or a fragment from \code{\link[=read_usdm]{read_usdm()}}, or NULL.}

\item{ars}{A path to an ARS JSON, or a fragment from \code{\link[=read_ars]{read_ars()}}, or NULL.}

\item{define}{A named list of define fragments/paths, e.g.
\code{list(adam = "adam/define.xml", sdtm = "sdtm/define.xml")}. Names set the
\code{standard} when a path is given.}

\item{ard}{A path/data.frame/fragment of results, or NULL.}

\item{endpoint_output_map}{Optional named list mapping endpoint source-ids to
output source-ids; overrides/augments the USDM fragment's own map.}
}
\value{
A \code{trace_graph}.
}
\description{
The centrepiece of traceR. Reads (or accepts pre-read fragments for) the
USDM study definition, the ARS Reporting Event, one or more define.xml
files, and optionally an ARD; merges them into one \code{\link[=trace_graph]{trace_graph()}}; and
resolves the cross-source edges that no single source expresses:
}
\details{
\itemize{
\item the \strong{endpoint to output bridge} (\code{evaluated_by}), taken from the USDM
fragment's \code{endpointOutputMap} when present, or from \code{endpoint_output_map};
\item \strong{stub materialisation} for lineage targets referenced by an edge but not
defined by any parsed source (e.g. an SDTM predecessor variable when only
the ADaM define was read) — so lineage is never silently dropped.
}

The result carries provenance and content hashes in \code{meta} for
reproducibility (see \code{\link[=trace_manifest]{trace_manifest()}}).
}
