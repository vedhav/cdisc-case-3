% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/trace_validate.R
\name{trace_gaps}
\alias{trace_gaps}
\title{Detect traceability gaps}
\usage{
trace_gaps(graph)
}
\arguments{
\item{graph}{A \code{\link[=trace_graph]{trace_graph()}}.}
}
\value{
A tibble with \code{id}, \code{type}, \code{gap}, \code{severity}.
}
\description{
Walks the graph for the structural holes that break objective-to-result
traceability, returning them as a tidy table so they can be triaged or
gated in a pipeline. Detected gaps include:
}
\details{
\itemize{
\item an objective with no endpoint;
\item an endpoint not linked to any output (\code{evaluated_by} missing);
\item an output that displays no analysis (\code{displayed_in} missing);
\item an analysis with no method, no population, or that produces no result;
\item an analysis whose \code{operates_on} variable is not defined in any define.xml
(an \emph{unbound} ADaM variable — present only as an inferred stub);
\item a \code{derived_from} predecessor variable referenced but never defined.
}
}
