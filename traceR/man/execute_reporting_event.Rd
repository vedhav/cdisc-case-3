% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/execute.R
\name{execute_reporting_event}
\alias{execute_reporting_event}
\title{Execute all supported analyses in a reporting event}
\usage{
execute_reporting_event(graph, adam_dir, ard_dir = NULL)
}
\arguments{
\item{graph}{A \code{\link[=trace_graph]{trace_graph()}}.}

\item{adam_dir}{Directory of ADaM CSVs.}

\item{ard_dir}{Optional directory to write \verb{<analysis_id>.csv} files.}
}
\value{
A list with \code{ard} (consolidated tibble) and \code{status} (tibble).
}
\description{
Loops the ARS analyses in the graph, executes the ones the built-in engine
supports (see \code{\link[=execute_analysis]{execute_analysis()}}), writes per-analysis ARDs if \code{ard_dir}
is given, and returns the consolidated long-skinny ARD plus a per-analysis
status table. Merge the results back with \verb{build_trace(..., ard = <ard>)} to
close the objective-to-result loop.
}
