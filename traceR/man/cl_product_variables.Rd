% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cdisc_library.R
\name{cl_product_variables}
\alias{cl_product_variables}
\title{Download an exhaustive variable catalogue for a product (metadata-driven)}
\usage{
cl_product_variables(product, version, key = cdisc_library_key())
}
\arguments{
\item{product}{Product slug, e.g. \code{"sdtmig"} or \code{"adamig"}.}

\item{version}{Version slug, e.g. \code{"3-4"}.}

\item{key}{A CDISC Library API key; defaults to \code{\link[=cdisc_library_key]{cdisc_library_key()}}.}
}
\value{
A tibble in the traceR snapshot schema (one row per variable).
}
\description{
Discovers the product's datasets by following the API's \verb{_links} (rather than
hardcoding a domain list), then pulls every variable of every dataset. Works
for SDTMIG (\verb{product = "sdtmig", version = "3-4"}) and ADaMIG
(\verb{product = "adamig", version = "1-3"}), among others.
}
